import {tv} from "tailwind-variants";

export const formContainer = tv({
    base: "flex-grow flex flex-wrap justify-center items-center flex-col max-w-2xl w-full mx-auto"
});

export const formLabel = tv({
    base: "text-xl"
})